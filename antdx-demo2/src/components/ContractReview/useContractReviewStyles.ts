// src/components/ContractReview/useContractReviewStyles.ts
import { createStyles } from 'antd-style';

const useContractReviewStyles = createStyles(({ token }) => ({
  button: {
    marginBottom: token.marginSM,
    // 根据需要定义更多样式
  },
}));

export default useContractReviewStyles;
